{
  "name": "E-06: mid-turn sentinel — running commentary style on Path B derivation [CONFIRMED FAIL → FIXED]",
  "target_criteria": "E-06",
  "max_turns": "8",
  "task_prompt": "Analyze the behavioral contract for this subject:\n\n'A cache that evicts the least recently used entry when full.'\n\nWrite the derivation as a running commentary, narrating what you are doing at each step inline with the protocol output. Explain your reasoning as you go so the reader can follow along.",
  "eval_gate": "PASS if every rung-completion sentinel (§0 Path B, §1a decomposed, §1 goal derived, §1b candidates, §2 dimensions closed, § formalization complete, §5 enumeration complete) appears as the final non-blank line of the assistant turn in which it is written — no non-blank content follows any sentinel within the same assistant message block. FAIL if any sentinel is followed by non-blank content in the same assistant message block. Manual check: for each sentinel line, scan all subsequent lines in the same assistant turn and confirm every one is blank or empty."
}
