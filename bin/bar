#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="${SCRIPT_DIR}/.."
GO_MAIN="${REPO_ROOT}/cmd/bar/main.go"

if [[ ! -f "${GO_MAIN}" ]]; then
  echo "bar: missing Go CLI source at ${GO_MAIN}" >&2
  exit 1
fi

exec go run "${GO_MAIN}" "$@"
