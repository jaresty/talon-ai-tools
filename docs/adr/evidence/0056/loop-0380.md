## loop-0380 red | helper:diff-snapshot=0 files changed
- command: |
    python3 - <<'PY'
    from pathlib import Path
    path = Path('.github/workflows/test.yml')
    lines = path.read_text(encoding='utf-8').splitlines()
    invalid = any(line.strip() == "printf '%s" for line in lines)
    if invalid:
        print("INVALID_PRINTF_QUOTING detected")
        raise SystemExit(1)
    print("workflow quoting ok")
    PY
- timestamp: 2025-12-23T16:23:41Z
- exit_status: 1
- output: |
    INVALID_PRINTF_QUOTING detected

## loop-0380 green | helper:diff-snapshot=1 file changed, 3 insertions(+), 6 deletions(-)
- command: *(same as above)*
- timestamp: 2025-12-23T16:24:03Z
- exit_status: 0
- output: |
    workflow quoting ok

## loop-0380 removal | helper:diff-snapshot=0 files changed
- command: |
    git checkout -- .github/workflows/test.yml && \
    python3 - <<'PY'
    from pathlib import Path
    path = Path('.github/workflows/test.yml')
    lines = path.read_text(encoding='utf-8').splitlines()
    invalid = any(line.strip() == "printf '%s" for line in lines)
    if invalid:
        print("INVALID_PRINTF_QUOTING detected")
        raise SystemExit(1)
    print("workflow quoting ok")
    PY
- timestamp: 2025-12-23T16:24:28Z
- exit_status: 1
- output: |
    INVALID_PRINTF_QUOTING detected
