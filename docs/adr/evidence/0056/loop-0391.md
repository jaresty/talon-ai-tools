## loop-0391 red | helper:diff-snapshot=0 files changed
- command: |
    python3 - <<'PY'
    from pathlib import Path
    text = Path('docs/adr/0056-concordance-personas-axes-history-gating.md').read_text(encoding='utf-8').lower()
    if 'remove any leftover `cli-warning-streak*.json` files manually' in text:
        print('cleanup note present')
    else:
        raise SystemExit('cleanup note missing')
    PY
- timestamp: 2025-12-23T17:58:41Z
- exit_status: 1
- output: |
    cleanup note missing

## loop-0391 green | helper:diff-snapshot=2 files changed, 22 insertions(+), 1 deletion(-)
- command: |
    python3 - <<'PY'
    from pathlib import Path
    text = Path('docs/adr/0056-concordance-personas-axes-history-gating.md').read_text(encoding='utf-8').lower()
    if 'remove any leftover `cli-warning-streak*.json` files manually' in text:
        print('cleanup note present')
    else:
        raise SystemExit('cleanup note missing')
    PY
- timestamp: 2025-12-23T17:59:18Z
- exit_status: 0
- output: |
    cleanup note present

## loop-0391 removal | helper:diff-snapshot=0 files changed
- command: |
    git restore --source=HEAD -- docs/adr/0056-concordance-personas-axes-history-gating.md docs/adr/0056-concordance-personas-axes-history-gating.work-log.md && \
    python3 - <<'PY'
    from pathlib import Path
    text = Path('docs/adr/0056-concordance-personas-axes-history-gating.md').read_text(encoding='utf-8').lower()
    if 'remove any leftover `cli-warning-streak*.json` files manually' in text:
        print('cleanup note present')
    else:
        raise SystemExit('cleanup note missing')
    PY
- timestamp: 2025-12-23T17:59:58Z
- exit_status: 1
- output: |
    cleanup note missing
