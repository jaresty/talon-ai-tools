# loop-0403

- helper_version: helper:v20251223.1
- focus: ADR 0058 §Implementation Plan 4 — refresh suggest meta-prompt to prioritise axis tokens and make presets optional
- riskiest_assumption: Updating the meta-prompt wording will steer the model toward axis-first stances without regressing JSON formatting; validated via a unit test that inspects the generated prompt text for the new guidance.
- validation_targets:
  - `python3 -m pytest _tests/test_gpt_actions.py::GPTActionPromptSessionTests::test_suggest_prompt_text_emphasises_axis_tokens`
- evidence:
  - red | 2025-12-23T20:48:20Z | exit 1 | `python3 -m pytest _tests/test_gpt_actions.py::GPTActionPromptSessionTests::test_suggest_prompt_text_emphasises_axis_tokens`
    - helper:diff-snapshot=0 files changed
    - pointer: inline — test failed because legacy prompt text lacked the new axis-token guidance.
  - green | 2025-12-23T20:54:15Z | exit 0 | `python3 -m pytest _tests/test_gpt_actions.py::GPTActionPromptSessionTests::test_suggest_prompt_text_emphasises_axis_tokens`
    - helper:diff-snapshot=2 files changed, 32 insertions(+), 15 deletions(-)
    - pointer: inline — updated prompt text now surfaces axis-first instructions and cautions against guessing presets.
- rollback_plan: `git restore --source=HEAD -- GPT/gpt.py _tests/test_gpt_actions.py` && `python3 -m pytest _tests/test_gpt_actions.py::GPTActionPromptSessionTests::test_suggest_prompt_text_emphasises_axis_tokens` (failure should reappear).
- delta_summary: helper:diff-snapshot=2 files changed, 32 insertions(+), 15 deletions(-)
- residual_risks:
  - Risk: Prompt instructions may become stale if the persona/intent token catalog shifts. Mitigation: schedule a periodic documentation review alongside axis catalog updates; Trigger: catalog changes in `persona_intent_maps`.
- next_work:
  - Behaviour: monitor live suggestion outputs after deployment — spot-check `model run suggest` results to confirm reasoning reflects the new guidance.
