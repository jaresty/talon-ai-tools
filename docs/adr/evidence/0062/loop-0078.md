- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – History Lifecycle Orchestrator (migrate telemetry export tests to façade)
- riskiest_assumption: Telemetry export tests would continue importing `requestLog`, weakening guarantees that the façade remains authoritative (probability medium, impact medium).
- validation_targets:
  - python3 -m pytest _tests/test_telemetry_export.py
- evidence:
  - red | 2025-12-26T16:10:05Z | exit 0 | git show HEAD:_tests/test_telemetry_export.py | rg "from talon_user.lib import requestLog"
    - helper:diff-snapshot=0 files changed
    - pointer: inline — prior tests imported `requestLog` directly
  - green | 2025-12-26T16:10:52Z | exit 0 | python3 -m pytest _tests/test_telemetry_export.py
    - helper:diff-snapshot=1 file changed, 0 insertions(+), 0 deletions(-)
    - pointer: inline — 8 passed
  - removal | 2025-12-26T16:11:03Z | exit 1 | rg "from talon_user.lib import requestLog" _tests/test_telemetry_export.py
    - helper:diff-snapshot=0 files changed
    - pointer: inline — import removed; tests now depend on `historyLifecycle`
- rollback_plan: git restore --source=HEAD -- _tests/test_telemetry_export.py && python3 -m pytest _tests/test_telemetry_export.py
- delta_summary: helper:diff-snapshot=1 file changed; telemetry export tests now rely on `historyLifecycle` helpers for history setup.
- residual_risks:
  - Some doctest fixtures may still reference `requestLog`; audit future loops if necessary.
- next_work:
  - Behaviour: review remaining test suites for direct `requestLog` usage and migrate as needed.
