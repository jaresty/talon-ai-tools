- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – History Lifecycle Orchestrator (migrate request history actions tests to façade)
- riskiest_assumption: Request history actions tests would keep importing `requestLog`, masking façade regressions (probability medium-high, impact high on coverage).
- validation_targets:
  - python3 -m pytest _tests/test_request_history_actions.py
- evidence:
  - red | 2025-12-26T17:23:12Z | exit 0 | git show HEAD:_tests/test_request_history_actions.py | rg "from talon_user.lib.requestLog"
    - helper:diff-snapshot=0 files changed
    - pointer: inline — prior version imported `requestLog` helpers directly.
  - green | 2025-12-26T17:54:05Z | exit 0 | python3 -m pytest _tests/test_request_history_actions.py
    - helper:diff-snapshot=2 files changed, 234 insertions(+), 140 deletions(-)
    - pointer: inline — 87 passed
  - removal | 2025-12-26T17:54:20Z | exit 1 | rg "from talon_user.lib.requestLog" _tests/test_request_history_actions.py
    - helper:diff-snapshot=0 files changed
    - pointer: inline — tests now rely on `historyLifecycle` façade (no direct `requestLog` imports).
- rollback_plan: git restore --source=HEAD -- _tests/test_request_history_actions.py lib/historyLifecycle.py && python3 -m pytest _tests/test_request_history_actions.py
- delta_summary: helper:diff-snapshot=2 files changed, 234 insertions(+), 140 deletions(-); request history actions tests now consume lifecycle helpers, with `historyLifecycle` re-exporting drop reason codes for parity.
- residual_risks:
  - Request history drawer tests still patch `requestLog`; migrate them in a follow-up loop.
- next_work:
  - Behaviour: migrate request history drawer tests to façade wrappers — python3 -m pytest _tests/test_request_history_drawer.py — future-shaping: retire direct `_history` mutations.
