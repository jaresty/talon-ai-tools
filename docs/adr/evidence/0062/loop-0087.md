- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – Guidance Surface Coordinator (Help Hub canonical axis tokens)
- riskiest_assumption: Help Hub would keep normalising persona axis tokens via legacy personaConfig helpers, so surfaces could diverge from the canonical persona orchestrator (probability medium, impact medium-high on Concordance visibility).
- validation_targets:
  - python3 -m pytest _tests/test_help_hub.py::test_help_hub_canonical_persona_token_uses_orchestrator
- evidence:
  - red | 2025-12-26T21:23:18Z | exit 1 | python3 -m pytest _tests/test_help_hub.py::test_help_hub_canonical_persona_token_uses_orchestrator
    - helper:diff-snapshot=1 file changed, 36 insertions(+)
    - pointer: inline — AssertionError: expected "canonical-axis-token" but received "mentor voice", proving Help Hub bypassed the orchestrator.
  - green | 2025-12-26T21:27:04Z | exit 0 | python3 -m pytest _tests/test_help_hub.py::test_help_hub_canonical_persona_token_uses_orchestrator
    - helper:diff-snapshot=2 files changed, 47 insertions(+)
    - pointer: inline — test passes after Help Hub delegates axis canonicalisation to `get_persona_intent_orchestrator()`.
  - removal | 2025-12-26T21:28:32Z | exit 1 | git stash push -- lib/helpHub.py && python3 -m pytest _tests/test_help_hub.py::test_help_hub_canonical_persona_token_uses_orchestrator && git stash pop
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting `lib/helpHub.py` restores the failure where `_canonical_persona_token` skips the orchestrator.
- rollback_plan: git stash push -- lib/helpHub.py && python3 -m pytest _tests/test_help_hub.py::test_help_hub_canonical_persona_token_uses_orchestrator && git stash pop
- delta_summary: Help Hub `_canonical_persona_token` now delegates to the persona orchestrator before falling back to legacy helpers so axis aliases stay aligned across surfaces and tests guard the shared contract (helper:diff-snapshot=2 files changed, 47 insertions(+)).
- residual_risks:
  - Other Help Hub helpers (intent buckets, persona metadata fallbacks) still consult personaConfig directly; monitor future loops to route those lookups through the orchestrator snapshot.
- next_work:
  - Behaviour: extend orchestrator usage to remaining Help Hub metadata helpers (e.g., `_intent_spoken_buckets`) — python3 -m pytest _tests/test_help_hub.py — future-shaping: converge all Help Hub persona metadata on the orchestrator facade.
