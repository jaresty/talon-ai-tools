- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – Guidance Surface Coordinator (Help Canvas intent presets)
- riskiest_assumption: Model Help canvas would keep hydrating intent presets via legacy personaConfig maps, letting Concordance-facing quick help drift from the canonical persona orchestrator (probability medium, impact medium-high on surface parity).
- validation_targets:
  - python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_presets_use_persona_orchestrator
- evidence:
  - red | 2025-12-26T21:31:18Z | exit 1 | python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_presets_use_persona_orchestrator
    - helper:diff-snapshot=1 file changed, 52 insertions(+), 1 deletion(-)
    - pointer: inline — AttributeError: `talon_user.lib.modelHelpCanvas` lacked `_get_persona_orchestrator`, proving the canvas could not be patched to the shared facade.
  - green | 2025-12-26T21:33:46Z | exit 0 | python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_presets_use_persona_orchestrator
    - helper:diff-snapshot=2 files changed, 83 insertions(+), 7 deletions(-)
    - pointer: inline — intent preset guard passes once Help canvas delegates to `_get_persona_orchestrator()` before consulting legacy maps.
  - removal | 2025-12-26T21:34:30Z | exit 1 | git stash push -- lib/modelHelpCanvas.py && python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_presets_use_persona_orchestrator && git stash pop
    - helper:diff-snapshot=0 files changed
    - pointer: inline — restoring the pre-loop module removes `_get_persona_orchestrator`, causing the guard to fail again.
- rollback_plan: git stash push -- lib/modelHelpCanvas.py && python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_presets_use_persona_orchestrator && git stash pop
- delta_summary: helper:diff-snapshot=2 files changed, 83 insertions(+), 7 deletions(-); Help canvas intent/persona helpers now call `_get_persona_orchestrator()` first, returning orchestrator presets with legacy personaConfig maps as a fallback.
- residual_risks:
  - `_intent_spoken_buckets` and `_normalize_intent` still rely on personaConfig; future loops can route those through the orchestrator for full canonical coverage.
- next_work:
  - Behaviour: migrate Help canvas intent normalisation helpers to the orchestrator — python3 -m pytest _tests/test_model_help_canvas.py — future-shaping: remove remaining personaConfig dependencies from canvas quick help.
