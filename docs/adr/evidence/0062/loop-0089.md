- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – Guidance Surface Coordinator (Help Hub intent buckets)
- riskiest_assumption: Help Hub would keep assembling intent buckets via legacy personaConfig helpers, letting Concordance-facing metadata drift from the persona orchestrator snapshot (probability medium, impact medium-high on surface parity).
- validation_targets:
  - python3 -m pytest _tests/test_help_hub.py::test_help_hub_intent_buckets_use_orchestrator
- evidence:
  - red | 2025-12-26T21:40:12Z | exit 1 | python3 -m pytest _tests/test_help_hub.py::test_help_hub_intent_buckets_use_orchestrator
    - helper:diff-snapshot=1 file changed, 108 insertions(+), 6 deletions(-)
    - pointer: inline — AssertionError showed buckets returned snapshot labels instead of orchestrator labels, proving the helper ignored the façade.
  - green | 2025-12-26T21:43:08Z | exit 0 | python3 -m pytest _tests/test_help_hub.py::test_help_hub_intent_buckets_use_orchestrator
    - helper:diff-snapshot=2 files changed, 155 insertions(+), 19 deletions(-)
    - pointer: inline — buckets now surface orchestrator labels, confirming the helper delegates to `get_persona_intent_orchestrator()` before legacy fallbacks.
  - removal | 2025-12-26T21:43:32Z | exit 1 | git stash push -- lib/helpHub.py && python3 -m pytest _tests/test_help_hub.py::test_help_hub_intent_buckets_use_orchestrator && git stash pop
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting `lib/helpHub.py` strips the orchestrator delegation and the guard test fails again, demonstrating the change is reversible.
- rollback_plan: git stash push -- lib/helpHub.py && python3 -m pytest _tests/test_help_hub.py::test_help_hub_intent_buckets_use_orchestrator && git stash pop
- delta_summary: helper:diff-snapshot=2 files changed, 155 insertions(+), 19 deletions(-); Help Hub intent bucket helper now pulls labels from the persona orchestrator before falling back to `personaCatalog`/`personaConfig`, keeping quick-help metadata aligned with the canonical façade.
- residual_risks:
  - Help Hub clipboard/export helpers still format intent metadata manually; future loops should reuse the orchestrator-backed bucket lookup there.
- next_work:
  - Behaviour: extend orchestrator usage to help canvas intent normalization helpers — python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_presets_use_persona_orchestrator — future-shaping: consolidate remaining personaConfig dependencies across canvases.
