- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – Guidance Surface Coordinator (Help Canvas intent normalization)
- riskiest_assumption: Help canvas quick help would continue normalising intent buckets and aliases via legacy personaConfig helpers, letting Concordance guidance drift from the persona orchestrator snapshot (probability medium, impact medium-high on visibility).
- validation_targets:
  - python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_spoken_buckets_use_persona_orchestrator
  - python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_normalize_intent_uses_persona_orchestrator
- evidence:
  - red | 2025-12-26T21:47:05Z | exit 1 | python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_spoken_buckets_use_persona_orchestrator
    - helper:diff-snapshot=1 file changed, 52 insertions(+), 3 deletions(-)
    - pointer: inline — AttributeError surfaced because `personaCatalog` was not exposed, showing the helper had no orchestrator pathway.
  - red | 2025-12-26T21:47:32Z | exit 1 | python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_normalize_intent_uses_persona_orchestrator
    - helper:diff-snapshot=1 file changed, 52 insertions(+), 3 deletions(-)
    - pointer: inline — AttributeError confirmed `_normalize_intent` could not be patched to the orchestrator.
  - green | 2025-12-26T21:52:12Z | exit 0 | python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_spoken_buckets_use_persona_orchestrator
    - helper:diff-snapshot=2 files changed, 131 insertions(+), 14 deletions(-)
    - pointer: inline — buckets now surface orchestrator labels with personaCatalog fallbacks.
  - green | 2025-12-26T21:52:19Z | exit 0 | python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_normalize_intent_uses_persona_orchestrator
    - helper:diff-snapshot=2 files changed, 131 insertions(+), 14 deletions(-)
    - pointer: inline — `_normalize_intent` returns the orchestrator canonical key before consulting legacy helpers.
  - removal | 2025-12-26T21:50:45Z | exit 1 | git stash push -- lib/modelHelpCanvas.py && python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_spoken_buckets_use_persona_orchestrator _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_normalize_intent_uses_persona_orchestrator && git stash pop
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting `lib/modelHelpCanvas.py` removes the orchestrator delegation and both guard tests fail again.
- rollback_plan: git stash push -- lib/modelHelpCanvas.py && python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_spoken_buckets_use_persona_orchestrator _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_normalize_intent_uses_persona_orchestrator && git stash pop
- delta_summary: helper:diff-snapshot=2 files changed, 131 insertions(+), 14 deletions(-); Help canvas intent bucket and normalization helpers now prioritise the persona orchestrator with personaCatalog/legacy fallbacks, keeping quick help in sync with Concordance metadata.
- residual_risks:
  - Canvas command builders still read persona intent maps directly when assembling spoken command hints; future work can reuse the orchestrator snapshot for consistency.
- next_work:
  - Behaviour: audit remaining help surface exports for personaConfig dependencies — python3 -m pytest _tests/test_help_hub.py — future-shaping: unify metadata helpers across canvases and exports.
