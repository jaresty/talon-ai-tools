- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – History Lifecycle Orchestrator (request history actions gating)
- riskiest_assumption: Request history actions would keep bespoke gating + notifications, diverging from surfaceGuidance canonical guard and leaving Concordance drop messaging inconsistent (probability high, impact medium on telemetry alignment).
- validation_targets:
  - python3 -m pytest _tests/test_request_history_actions.py
- evidence:
  - red | 2025-12-27T04:31:30Z | exit 1 | python3 -m pytest _tests/test_request_history_actions.py::RequestHistoryActionTests::test_history_actions_reject_if_in_flight_notifies_with_drop_message
    - helper:diff-snapshot=0 files changed
    - pointer: inline — test fails asserting guard lacks on_block callback, confirming legacy gating path.
  - green | 2025-12-27T04:33:40Z | exit 0 | python3 -m pytest _tests/test_request_history_actions.py
    - helper:diff-snapshot=2 files changed, 67 insertions(+), 13 deletions(-)
    - pointer: inline — suite passes with guard on_block wiring and fallback notifications.
  - removal | 2025-12-27T04:34:20Z | exit 1 | git stash push --keep-index --message loop-0107-removal && python3 -m pytest _tests/test_request_history_actions.py
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting code while retaining tests reintroduces missing on_block wiring (AttributeError from patched guard expectations).
- rollback_plan: git stash push --keep-index --message loop-0107-removal && python3 -m pytest _tests/test_request_history_actions.py && git stash pop && git reset _tests/test_request_history_actions.py
- delta_summary: helper:diff-snapshot=2 files changed, 67 insertions(+), 13 deletions(-); request history actions now delegate gating via surfaceGuidance with on_block fallback notifications.
- residual_risks:
  - Provider and pattern GUI modules still maintain bespoke gating; migrate to surfaceGuidance in subsequent loops; monitor via `_tests/test_provider_commands.py` and `_tests/test_model_pattern_gui_guard.py`.
- next_work:
  - Behaviour: port provider/model GUI gating to surfaceGuidance — python3 -m pytest _tests/test_provider_commands.py — future-shaping: ensure all GUI surfaces share canonical guard contracts.
