- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – Guidance Surface Coordinator (provider commands gating)
- riskiest_assumption: Provider command actions would retain bespoke gating instead of surfaceGuidance, leaving Concordance messaging inconsistent (probability high, impact medium on provider UX).
- validation_targets:
  - python3 -m pytest _tests/test_provider_commands.py
- evidence:
  - red | 2025-12-27T04:35:00Z | exit 1 | python3 -m pytest _tests/test_provider_commands.py::ProviderCommandGuardTests::test_reject_if_request_in_flight_notifies_with_drop_message
    - helper:diff-snapshot=0 files changed
    - pointer: inline — guard patch fails because module lacks guard_surface_request delegation.
  - green | 2025-12-27T04:37:20Z | exit 0 | python3 -m pytest _tests/test_provider_commands.py
    - helper:diff-snapshot=2 files changed, 64 insertions(+), 69 deletions(-)
    - pointer: inline — suite passes using surfaceGuidance on_block fallback messaging.
  - removal | 2025-12-27T04:37:50Z | exit 1 | git stash push --keep-index --message loop-0108-removal && python3 -m pytest _tests/test_provider_commands.py
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting providerCommands while keeping updated tests restores missing guard_surface_request attribute failure.
- rollback_plan: git stash push --keep-index --message loop-0108-removal && python3 -m pytest _tests/test_provider_commands.py && git stash pop && git reset _tests/test_provider_commands.py
- delta_summary: helper:diff-snapshot=2 files changed, 64 insertions(+), 69 deletions(-); provider commands now route gating via surfaceGuidance with fallback notifications.
- residual_risks:
  - Model pattern/prompt GUIs still duplicate gating helpers; plan next loop against `_tests/test_model_pattern_gui_guard.py` and `_tests/test_model_prompt_pattern_gui_guard.py`.
- next_work:
  - Behaviour: convert pattern GUI gating to surfaceGuidance — python3 -m pytest _tests/test_model_pattern_gui_guard.py — future-shaping: ensure remaining UI surfaces share canonical guard contract.
