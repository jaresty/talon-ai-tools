- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – Prompt Persona Orchestrator (hydrated values doc hygiene)
- riskiest_assumption: The contributor crib sheet still pointed authors directly at `lib.personaConfig` without mentioning the orchestrator façade, encouraging bypass of the shared entrypoint.
- validation_targets:
  - python3 - <<'PY'
from pathlib import Path
text = Path('docs/adding-hydrated-values.md').read_text()
if 'persona orchestrator' in text.lower():
    raise SystemExit(0)
raise SystemExit(1)
PY
- evidence:
  - red | 2026-01-01T01:54:00Z | exit 1 | python3 - <<'PY' … (hydrated values orchestrator check)
    - helper:diff-snapshot=0 files changed
    - pointer: inline — crib sheet referenced only `lib.personaConfig` for persona/intent updates.
  - green | 2026-01-01T01:55:43Z | exit 0 | python3 - <<'PY' … (hydrated values orchestrator check)
    - helper:diff-snapshot=1 file changed, 3 insertions(+), 1 deletion(-)
    - pointer: inline — crib sheet now mentions `get_persona_intent_orchestrator()` and cache reset guidance.
- rollback_plan: git restore --source=HEAD -- docs/adding-hydrated-values.md docs/adr/0062-canonicalize-gpt-surface-orchestrators.work-log.md
- delta_summary: helper:diff-snapshot=1 file changed, 3 insertions(+), 1 deletion(-); hydrated values guide now references the persona orchestrator façade.
- residual_risks:
  - README/CONTRIBUTING may still mention raw `personaConfig`; address in the next loop.
- next_work:
  - Documentation: update README/CONTRIBUTING orchestrator guidance — python3 - <<'PY' … upcoming check — future-shaping: ensure contributor docs point to the façade.
