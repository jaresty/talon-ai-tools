- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – Guidance Surface Coordinator (Help canvas catalog fallback)
- riskiest_assumption: Help canvas would fall back to legacy personaConfig maps when the orchestrator is unavailable, drifting Concordance quick help from the canonical catalog snapshot.
- validation_targets:
  - python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_presets_catalog_fallback_without_persona_maps
  - python3 -m pytest _tests/test_model_help_canvas.py
- evidence:
  - red | 2026-01-01T06:06:40Z | exit 1 | python3 -m pytest _tests/test_model_help_canvas.py::ModelHelpCanvasTests::test_intent_presets_catalog_fallback_without_persona_maps
    - helper:diff-snapshot=2 files changed, 184 insertions(+), 64 deletions(-)
    - pointer: inline — new guard test failed because `_intent_presets` still fetched `persona_intent_maps` instead of the catalog snapshot when the orchestrator was unavailable.
  - green | 2026-01-01T06:09:03Z | exit 0 | python3 -m pytest _tests/test_model_help_canvas.py
    - helper:diff-snapshot=2 files changed, 184 insertions(+), 64 deletions(-)
    - pointer: inline — Help canvas command helpers now pull presets and display metadata from `personaCatalog.get_persona_intent_catalog()`, and the suite passes.
- rollback_plan: git restore --source=HEAD -- _tests/test_model_help_canvas.py lib/modelHelpCanvas.py && python3 -m pytest _tests/test_model_help_canvas.py
- delta_summary: helper:diff-snapshot=2 files changed, 184 insertions(+), 64 deletions(-); Help canvas persona/intent fallbacks reuse the catalog snapshot before touching legacy helpers, with new guards covering the outage path.
- residual_risks:
  - Help Hub metadata fallbacks still depend on personaConfig maps; align them with the catalog snapshot next.
- next_work:
  - Behaviour: align Help Hub metadata fallbacks with the catalog snapshot — python3 -m pytest _tests/test_help_hub.py — future-shaping: ensure documentation surfaces stay in sync with the orchestrator during outages.
