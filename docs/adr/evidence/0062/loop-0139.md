- helper_version: helper:v20251223.1
- focus: ADR-0062 §Refactor Plan – Guidance Surface Coordinator (Help Hub metadata fallback)
- riskiest_assumption: Without catalog fallbacks, Help Hub cheat sheet keeps emitting legacy personaConfig metadata when the orchestrator is unavailable, hiding canonical personas/intent aliases (probability medium, impact medium-high on Concordance visibility).
- validation_targets:
  - python3 -m pytest _tests/test_help_hub.py::test_cheat_sheet_catalog_fallback_without_maps
- evidence:
  - red | 2026-01-01T06:31:40Z | exit 1 | python3 -m pytest _tests/test_help_hub.py::test_cheat_sheet_catalog_fallback_without_maps
    - helper:diff-snapshot=1 file changed, 86 insertions(+)
    - pointer: inline — cheat sheet still prints legacy personaConfig values ("Legacy Mentor") when the orchestrator is unavailable.
  - green | 2026-01-01T06:32:51Z | exit 0 | python3 -m pytest _tests/test_help_hub.py::test_cheat_sheet_catalog_fallback_without_maps
    - helper:diff-snapshot=2 files changed, 102 insertions(+), 10 deletions(-)
    - pointer: inline — cheat sheet now surfaces catalog-backed persona and intent metadata despite the orchestrator being offline.
  - removal | 2026-01-01T06:32:10Z | exit 1 | git stash push -- lib/helpHub.py && python3 -m pytest _tests/test_help_hub.py::test_cheat_sheet_catalog_fallback_without_maps && git stash pop
    - helper:diff-snapshot=1 file changed, 86 insertions(+)
    - pointer: inline — stashing the Help Hub module restores the legacy personaConfig output and the guard fails again.
- rollback_plan: git stash push -- lib/helpHub.py && python3 -m pytest _tests/test_help_hub.py::test_cheat_sheet_catalog_fallback_without_maps && git stash pop
- delta_summary: helper:diff-snapshot=2 files changed, 102 insertions(+), 10 deletions(-); reorder Help Hub cheat sheet fallbacks so persona and intent metadata use the catalog snapshot before legacy maps when the orchestrator is unavailable, and guard the behavior with a focused pytest.
- residual_risks:
  - Help Hub cheat sheet still surfaces catalog entries in insertion order; consider deterministic sorting if downstream consumers require stable ordering.
- next_work:
  - Behaviour: align helpDomain metadata fallback with persona catalog snapshot — python3 -m pytest _tests/test_help_domain.py — future-shaping: ensure documentation exports share the same outage-safe fallback.
