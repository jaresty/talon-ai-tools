- helper_version: helper:v20251223.1
- focus: ADR-0063 §Implementation Plan – Go CLI Core (documentation clarity for `bar` entrypoints)
- riskiest_assumption: README still references legacy `talon` entrypoints, confusing contributors about the new `bar` binary (probability medium, impact medium on onboarding).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'future Go entrypoints (e.g., bar' not in text:
          raise SystemExit('README still references legacy talon entrypoints')
      print('CLI README entrypoint comment references bar')
    PY
- evidence:
  - red | 2026-01-01T16:08:02Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'future Go entrypoints (e.g., bar' not in text:
          raise SystemExit('README still references legacy talon entrypoints')
      print('CLI README entrypoint comment references bar')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — README still mentioned `talon` entrypoints.
  - green | 2026-01-01T16:08:45Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'future Go entrypoints (e.g., bar' not in text:
          raise SystemExit('README still references legacy talon entrypoints')
      print('CLI README entrypoint comment references bar')
    PY
    - helper:diff-snapshot=1 file changed, 1 insertion(+), 1 deletion(-)
    - pointer: inline — README now references `bar` entrypoints.
  - removal | 2026-01-01T16:09:24Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'future Go entrypoints (e.g., bar' not in text:
          raise SystemExit('README still references legacy talon entrypoints')
      print('CLI README entrypoint comment references bar')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting the doc reintroduces the legacy `talon` wording.
- rollback_plan: git checkout HEAD -- cli/README.md
- delta_summary: helper:diff-snapshot=1 file changed, 1 insertion(+), 1 deletion(-); updated the README entrypoint comment to reference the `bar` binary rather than legacy `talon` names.
- loops_remaining_forecast: 1 loop remaining (adapter integration blocker capture); confidence medium.
- residual_risks:
  - Other docs may still reference `talon` binary; audit broader documentation in future loops.
- next_work:
  - Behaviour: capture adapter integration blocker per ADR-0063 Talon Adapter Layer — python3 - <<'PY' ...> — future-shaping: document the absence of adapter hooks pending CLI integration.
