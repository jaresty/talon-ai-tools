- helper_version: helper:v20251223.1
- focus: ADR-0063 §Implementation Plan – Go CLI Core (bar CLI feature flag plumbing)
- riskiest_assumption: Without a dedicated feature flag, we cannot gate delegation to the `bar` CLI for incremental rollout (probability high, impact high on parity safety).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/talonSettings.py').read_text()
      if 'bar_cli_enabled' not in text:
          raise SystemExit('missing bar_cli_enabled setting')
      print('bar_cli_enabled setting present')
    PY
- evidence:
  - red | 2026-01-01T16:08:02Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/talonSettings.py').read_text()
      if 'bar_cli_enabled' not in text:
          raise SystemExit('missing bar_cli_enabled setting')
      print('bar_cli_enabled setting present')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — setting absent so command exits with "missing bar_cli_enabled setting".
  - green | 2026-01-01T16:08:45Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/talonSettings.py').read_text()
      if 'bar_cli_enabled' not in text:
          raise SystemExit('missing bar_cli_enabled setting after edit')
      print('bar_cli_enabled setting present')
    PY
    - helper:diff-snapshot=2 files changed, 30 insertions(+), 4 deletions(-)
    - pointer: inline — helper reports the setting present after adding plumbing.
  - removal | 2026-01-01T16:09:24Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/talonSettings.py').read_text()
      if 'bar_cli_enabled' not in text:
          raise SystemExit('missing bar_cli_enabled setting (expected removal)')
      print('bar_cli_enabled setting present')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting the files reintroduces the missing-setting failure.
- rollback_plan: git checkout HEAD -- lib/talonSettings.py lib/providerCommands.py
- delta_summary: helper:diff-snapshot=2 files changed, 30 insertions(+), 4 deletions(-); added `bar_cli_enabled` setting and helper to read it.
- loops_remaining_forecast: 1 loop remaining (document feature flag usage); confidence medium.
- residual_risks:
  - Flag currently unused; upcoming work needs to hook provider commands to respect it.
- next_work:
  - Behaviour: document the feature flag and rollout steps — python3 - <<'PY' ...> — future-shaping: ensure onboarding materials highlight the new toggle.
