# loop-0010 establish work-log separation

## loop-0010 red | helper:rerun python3 -c "import pathlib,sys; path=pathlib.Path('docs/adr/0063-go-cli-single-source-of-truth.work-log.md'); print('exists' if path.exists() else 'missing'); sys.exit(0 if path.exists() else 1)"
- 2026-01-02T19:05:24Z | exit 1
- helper:diff-snapshot=0 files changed
- failure: work-log file missing; ADR lacks separate progress tracker | inline

```
missing
```

## loop-0010 green | helper:rerun python3 -c "import pathlib,sys; path=pathlib.Path('docs/adr/0063-go-cli-single-source-of-truth.work-log.md'); print('exists' if path.exists() else 'missing'); sys.exit(0 if path.exists() else 1)"
- 2026-01-02T19:07:13Z | exit 0
- helper:diff-snapshot=1 file changed, 14 insertions(+)
- work-log created alongside ADR capturing open behaviours and completed loops | inline

```
exists
```

## loop-0010 removal | helper:rerun mv docs/adr/0063-go-cli-single-source-of-truth.work-log.md docs/adr/0063-go-cli-single-source-of-truth.work-log.md.loopbak && python3 -c "import pathlib,sys; path=pathlib.Path('docs/adr/0063-go-cli-single-source-of-truth.work-log.md'); print('exists' if path.exists() else 'missing'); sys.exit(0 if path.exists() else 1)"
- 2026-01-02T19:07:19Z | exit 1
- helper:diff-snapshot=0 files changed
- failure returns once work-log temporarily removed, confirming guardrail | inline

```
missing
```
