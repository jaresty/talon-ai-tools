- helper_version: helper:v20251223.1
- focus: ADR-0063 §Implementation Plan – Talon Adapter Layer (document feature flag in ADR text)
- riskiest_assumption: ADR prose omits the `user.bar_cli_enabled` toggle, leaving governance guidance incomplete (probability medium, impact medium on rollout clarity).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'bar_cli_enabled' not in text:
          raise SystemExit('bar_cli_enabled missing in ADR text')
      print('ADR references bar_cli_enabled feature flag now')
    PY
- evidence:
  - red | 2026-01-01T17:23:39Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'bar_cli_enabled' not in text:
          raise SystemExit('bar_cli_enabled missing in ADR text')
      print('ADR references bar_cli_enabled feature flag now')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — ADR lacked the feature flag reference.
  - green | 2026-01-01T17:24:23Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'bar_cli_enabled' not in text:
          raise SystemExit('bar_cli_enabled missing in ADR after edit')
      print('ADR references bar_cli_enabled feature flag now')
    PY
    - helper:diff-snapshot=1 file changed, 1 insertion(+)
    - pointer: inline — ADR now mentions `user.bar_cli_enabled`.
  - removal | 2026-01-01T17:24:07Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'bar_cli_enabled' not in text:
          raise SystemExit('bar_cli_enabled missing in ADR (expected removal)')
      print('ADR references bar_cli_enabled feature flag now')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting the ADR removes the feature flag mention.
- rollback_plan: git checkout HEAD -- docs/adr/0063-go-cli-single-source-of-truth.md
- delta_summary: helper:diff-snapshot=1 file changed, 1 insertion(+); ADR prose now references `user.bar_cli_enabled` for adapter rollout.
- loops_remaining_forecast: 1 loop remaining (implement adapter stub); confidence medium.
- residual_risks:
  - Need adapter implementation to leverage the flag.
- next_work:
  - Behaviour: add bar CLI adapter stub — python3 -m pytest _tests/test_gpt_actions.py — future-shaping: ensure Talon pathways exercise the CLI.
