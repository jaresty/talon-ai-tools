- helper_version: helper:v20251223.1
- focus: ADR-0063 §Implementation Plan – Go CLI Core (document CLI path override and guardrails)
- riskiest_assumption: Without documentation, contributors may not know how to supply custom CLI paths or monitor delegation telemetry (probability medium, impact medium on rollout coordination).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'BAR_CLI_PATH' not in text:
          raise SystemExit('README missing BAR_CLI_PATH mention')
      print('README mentions BAR_CLI_PATH')
    PY
- evidence:
  - red | 2026-01-01T17:44:02Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'BAR_CLI_PATH' in text:
          raise SystemExit('README already mentions BAR_CLI_PATH')
      print('README missing BAR_CLI_PATH (expected red evidence)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — README lacked the environment override before the edit.
  - green | 2026-01-01T17:45:10Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'BAR_CLI_PATH' not in text:
          raise SystemExit('README missing BAR_CLI_PATH mention after update')
      print('README mentions BAR_CLI_PATH')
    PY
    - helper:diff-snapshot=2 files changed, 2 insertions(+)
    - pointer: inline — README now documents the override and ADR records the guardrail guidance.
  - removal | 2026-01-01T17:44:26Z | exit 0 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD~1:cli/README.md'], text=True)
      if 'BAR_CLI_PATH' in content:
          raise SystemExit('BAR_CLI_PATH unexpectedly present in HEAD~1')
      print('BAR_CLI_PATH missing in HEAD~1 (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — simulating a revert drops the documentation, recreating the failure.
- rollback_plan: git checkout HEAD -- cli/README.md docs/adr/0063-go-cli-single-source-of-truth.md
- delta_summary: helper:diff-snapshot=2 files changed, 2 insertions(+); Feature Flag docs mention `BAR_CLI_PATH` and ADR outlines CLI path overrides.
- loops_remaining_forecast: 0 loops remaining for documentation updates; confidence medium-high.
- residual_risks:
  - Further guidance needed once CLI emits structured telemetry; track in future loops.
- next_work:
  - Behaviour: add telemetry parsing and guardrail coverage — python3 - <<'PY' ...> — future-shaping: ensure CLI outputs are monitored.
