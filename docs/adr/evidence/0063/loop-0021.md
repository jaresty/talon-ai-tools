- helper_version: helper:v20251223.1
- focus: ADR-0063 §Talon Adapter Layer & Shared Contracts (document CLI JSON payload semantics)
- riskiest_assumption: Without documenting JSON payload fields, contributors could emit incompatible CLI responses, breaking Talon adapters (probability medium, impact medium-high on parity).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'JSON notify payload' not in text:
          raise SystemExit('README missing JSON delegation notes')
      print('README covers JSON delegation')
    PY
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'CLI stdout JSON contract' not in text:
          raise SystemExit('ADR missing CLI stdout JSON contract guidance')
      print('ADR covers CLI stdout JSON contract guidance')
    PY
- evidence:
  - red | 2026-01-02T00:17:02Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'JSON notify payload' in text:
          raise SystemExit('JSON notify payload already documented')
      raise SystemExit('cli README missing JSON notify payload guidance')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — README lacked JSON payload guidance before the edit.
  - red | 2026-01-02T00:17:15Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'CLI stdout JSON contract' in text:
          raise SystemExit('ADR already mentions JSON notify payload guidance')
      raise SystemExit('ADR missing JSON notify payload guidance')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — ADR did not mention the stdout JSON contract baseline.
  - green | 2026-01-02T00:19:05Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'JSON notify payload' not in text:
          raise SystemExit('README missing JSON notify payload guidance after update')
      print('README documents JSON notify payload guidance')
    PY
    - helper:diff-snapshot=3 files changed, 48 insertions(+), 1 deletion(-)
    - pointer: inline — README now documents the JSON notify payload semantics.
  - green | 2026-01-02T00:19:12Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'CLI stdout JSON contract' not in text:
          raise SystemExit('ADR missing CLI stdout JSON contract guidance after update')
      print('ADR documents CLI stdout JSON contract guidance')
    PY
    - helper:diff-snapshot=3 files changed, 48 insertions(+), 1 deletion(-)
    - pointer: inline — ADR references the stdout JSON contract fields.
  - removal | 2026-01-02T00:19:20Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:cli/README.md'], text=True)
      if 'JSON notify payload' in content:
          raise SystemExit('JSON notify payload already present in HEAD baseline')
      raise SystemExit('README baseline lacks JSON notify payload guidance (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting to the previous revision removes the JSON notify payload guidance.
  - removal | 2026-01-02T00:19:22Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:docs/adr/0063-go-cli-single-source-of-truth.md'], text=True)
      if 'CLI stdout JSON contract' in content:
          raise SystemExit('CLI stdout JSON contract already present in HEAD baseline')
      raise SystemExit('ADR baseline lacks CLI stdout JSON contract guidance (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting to the previous revision removes the CLI stdout JSON contract note.
- rollback_plan: git checkout HEAD -- cli/README.md docs/adr/0063-go-cli-single-source-of-truth.md docs/adr/0063-go-cli-single-source-of-truth.work-log.md
- delta_summary: helper:diff-snapshot=3 files changed, 48 insertions(+), 1 deletion(-); CLI README now spells out JSON notify payload semantics and ADR shared contracts cite the stdout JSON contract.
- loops_remaining_forecast: 1 loop remaining (telemetry residual); confidence medium.
- residual_risks:
  - Telemetry/drop-reason JSON fields remain to be documented once the CLI emits them consistently.
- next_work:
  - Behaviour: capture telemetry roadmap residual risk per ADR §Operational Mitigations — python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.work-log.md').read_text()
      if 'telemetry roadmap' not in text:
          raise SystemExit('work log missing telemetry roadmap note')
      print('Work log captures telemetry roadmap note')
    PY — future-shaping: ensure residual risks remain visible until telemetry integration lands.
