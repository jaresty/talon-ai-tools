- helper_version: helper:v20251223.1
- focus: ADR-0063 §Talon Adapter Layer (test CLI error payload handling)
- riskiest_assumption: Without guardrail tests, error payload handling could regress silently (probability medium, impact high on UX parity).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('_tests/test_provider_commands.py').read_text()
      if 'test_delegate_handles_error_payload' not in text:
          raise SystemExit('error payload test missing')
      print('error payload test present')
    PY
- evidence:
  - red | 2026-01-02T00:26:38Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('_tests/test_provider_commands.py').read_text()
      if 'test_delegate_handles_error_payload' in text:
          raise SystemExit('error payload test already present')
      raise SystemExit('error payload test missing')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — baseline tests lacked error payload coverage.
  - green | 2026-01-02T00:27:52Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('_tests/test_provider_commands.py').read_text()
      if 'test_delegate_handles_error_payload' not in text:
          raise SystemExit('error payload test missing after update')
      print('error payload test present')
    PY
    - helper:diff-snapshot=1 file changed, 30 insertions(+)
    - pointer: inline — new test verifies notify/log behaviour; supporting run: `python3 -m pytest _tests/test_provider_commands.py` (13 passed).
  - removal | 2026-01-02T00:27:59Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:_tests/test_provider_commands.py'], text=True)
      if 'test_delegate_handles_error_payload' in content:
          raise SystemExit('error payload test already present in HEAD baseline')
      raise SystemExit('baseline missing error payload test (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting to baseline removes the guardrail test and recreates the red failure.
- rollback_plan: git checkout HEAD -- _tests/test_provider_commands.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md
- delta_summary: helper:diff-snapshot=1 file changed, 30 insertions(+); added `test_delegate_handles_error_payload` covering notify/log expectations, confirmed with `python3 -m pytest _tests/test_provider_commands.py` (13 passed).
- loops_remaining_forecast: 5 loops remaining (error docs, payload helper refactor, helper tests, doc wrap-up); confidence medium.
- residual_risks:
  - Need integration tests that exercise real CLI binaries emitting error payloads.
- next_work:
  - Behaviour: document CLI error handling expectations — python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'CLI error payload' not in text:
          raise SystemExit('README missing CLI error payload guidance')
      print('README documents CLI error payload guidance')
    PY — future-shaping: align docs so contributors surface error handling semantics.
