- helper_version: helper:v20251223.1
- focus: ADR-0063 §Talon Adapter Layer & Shared Contracts (document CLI error payload handling)
- riskiest_assumption: Without documentation, contributors may omit error payload fields or expect legacy fallbacks, breaking guardrail parity (probability medium, impact high on UX/governance).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'CLI error payload' not in text:
          raise SystemExit('README missing CLI error payload guidance')
      print('README documents CLI error payload guidance')
    PY
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'CLI error payload' not in text:
          raise SystemExit('ADR missing CLI error payload guidance')
      print('ADR documents CLI error payload guidance')
    PY
- evidence:
  - red | 2026-01-02T00:28:45Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'CLI error payload' in text:
          raise SystemExit('README already documents CLI error payload')
      raise SystemExit('README missing CLI error payload guidance')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — README lacked error payload guidance before the edit.
  - red | 2026-01-02T00:28:52Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'CLI error payload' in text:
          raise SystemExit('ADR already documents CLI error payload guidance')
      raise SystemExit('ADR missing CLI error payload guidance')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — ADR lacked guidance on error payload handling baseline.
  - green | 2026-01-02T00:29:34Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text()
      if 'CLI error payload' not in text:
          raise SystemExit('README missing CLI error payload guidance after update')
      print('README documents CLI error payload guidance')
    PY
    - helper:diff-snapshot=2 files changed, 3 insertions(+), 3 deletions(-)
    - pointer: inline — README describes how error payloads trigger notify and suppress legacy fallbacks.
  - green | 2026-01-02T00:29:40Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'CLI error payload' not in text:
          raise SystemExit('ADR missing CLI error payload guidance after update')
      print('ADR documents CLI error payload guidance')
    PY
    - helper:diff-snapshot=2 files changed, 3 insertions(+), 3 deletions(-)
    - pointer: inline — ADR adapter responsibilities now reference CLI error payload semantics.
  - removal | 2026-01-02T00:29:46Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:cli/README.md'], text=True)
      if 'CLI error payload' in content:
          raise SystemExit('CLI error payload guidance already in HEAD baseline')
      raise SystemExit('README baseline lacks CLI error payload guidance (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting README removes the error payload guidance and recreates the red failure.
  - removal | 2026-01-02T00:29:50Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:docs/adr/0063-go-cli-single-source-of-truth.md'], text=True)
      if 'CLI error payload' in content:
          raise SystemExit('CLI error payload guidance already in ADR baseline')
      raise SystemExit('ADR baseline lacks CLI error payload guidance (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting ADR removes the error payload note and recreates the red failure.
- rollback_plan: git checkout HEAD -- cli/README.md docs/adr/0063-go-cli-single-source-of-truth.md docs/adr/0063-go-cli-single-source-of-truth.work-log.md
- delta_summary: helper:diff-snapshot=2 files changed, 3 insertions(+), 3 deletions(-); CLI README and ADR now document CLI error payload handling and legacy suppression expectations.
- loops_remaining_forecast: 4 loops remaining (payload helper refactor, helper tests, doc wrap-up); confidence medium.
- residual_risks:
  - Mapping CLI error payloads to Concordance drop-reason taxonomy remains future work (tracked in telemetry roadmap risk).
- next_work:
  - Behaviour: refactor CLI payload parsing into helper for reuse — python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/providerCommands.py').read_text()
      if '_parse_bar_cli_payload' not in text:
          raise SystemExit('payload parser helper missing')
      print('payload parser helper present')
    PY — future-shaping: centralise payload handling ahead of telemetry handshake.
