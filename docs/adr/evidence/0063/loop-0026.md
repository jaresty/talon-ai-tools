- helper_version: helper:v20251223.1
- focus: ADR-0063 §Talon Adapter Layer (centralise CLI payload parsing)
- riskiest_assumption: Without a shared parser helper, future telemetry fields will duplicate logic and drift across call sites (probability medium, impact medium-high on maintainability).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/providerCommands.py').read_text()
      if '_parse_bar_cli_payload' not in text:
          raise SystemExit('payload parser helper missing')
      print('payload parser helper present')
    PY
- evidence:
  - red | 2026-01-02T00:30:28Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/providerCommands.py').read_text()
      if '_parse_bar_cli_payload' in text:
          raise SystemExit('payload parser helper already present')
      raise SystemExit('payload parser helper missing')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — code base lacked a reusable CLI payload parser before the refactor.
  - green | 2026-01-02T00:32:01Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/providerCommands.py').read_text()
      if '_parse_bar_cli_payload' not in text:
          raise SystemExit('payload parser helper missing after refactor')
      print('payload parser helper present')
    PY
    - helper:diff-snapshot=1 file changed, 27 insertions(+), 12 deletions(-)
    - pointer: inline — helper centralises notify/error/debug extraction; supporting run: `python3 -m pytest _tests/test_provider_commands.py` (13 passed).
  - removal | 2026-01-02T00:32:08Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:lib/providerCommands.py'], text=True)
      if '_parse_bar_cli_payload' in content:
          raise SystemExit('payload parser helper already present in HEAD baseline')
      raise SystemExit('baseline missing payload parser helper (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting to the previous commit removes the helper and recreates the red failure.
- rollback_plan: git checkout HEAD -- lib/providerCommands.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md
- delta_summary: helper:diff-snapshot=1 file changed, 27 insertions(+), 12 deletions(-); introduced `_parse_bar_cli_payload` and routed `_delegate_to_bar_cli` through it while preserving behaviour.
- loops_remaining_forecast: 3 loops remaining (helper tests, documentation wrap-up); confidence medium.
- residual_risks:
  - Helper currently returns untyped dict payloads; telemetry schema alignment still pending.
- next_work:
  - Behaviour: extend tests to cover the new helper — python3 - <<'PY'
      from pathlib import Path
      text = Path('_tests/test_provider_commands.py').read_text()
      if 'test_parse_bar_cli_payload' not in text:
          raise SystemExit('payload helper tests missing')
      print('payload helper tests present')
    PY — future-shaping: ensure helper stays in sync with CLI fields.
