- helper_version: helper:v20251223.1
- focus: ADR-0063 §Talon Adapter Layer (introduce BarCliPayload dataclass)
- riskiest_assumption: Without a structured payload helper, CLI field growth will duplicate parsing logic and risk drift (probability medium, impact medium-high on maintainability).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/providerCommands.py').read_text()
      if 'BarCliPayload' not in text:
          raise SystemExit('payload dataclass missing')
      from types import SimpleNamespace
      import sys
      sys.path.insert(0, str(Path('.').resolve()))
      from bootstrap import bootstrap
      bootstrap()
      from talon_user.lib import providerCommands
      payload = providerCommands._parse_bar_cli_payload(SimpleNamespace(stdout='{"notify":"hi"}'))
      if not hasattr(payload, 'raw') or payload.notice != 'hi':
          raise SystemExit('BarCliPayload dataclass not returning expected values')
      print('BarCliPayload dataclass active')
    PY
- evidence:
  - red | 2026-01-02T00:36:12Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/providerCommands.py').read_text()
      if 'BarCliPayload' in text:
          raise SystemExit('payload dataclass already present')
      raise SystemExit('payload dataclass missing')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — confirmed absence of the dataclass before edits.
  - green | 2026-01-02T00:39:05Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/providerCommands.py').read_text()
      if 'BarCliPayload' not in text:
          raise SystemExit('payload dataclass missing')
      from types import SimpleNamespace
      import sys
      sys.path.insert(0, str(Path('.').resolve()))
      from bootstrap import bootstrap
      bootstrap()
      from talon_user.lib import providerCommands
      payload = providerCommands._parse_bar_cli_payload(SimpleNamespace(stdout='{"notify":"hi"}'))
      if not hasattr(payload, 'raw') or payload.notice != 'hi':
          raise SystemExit('BarCliPayload dataclass not returning expected values')
      print('BarCliPayload dataclass active')
    PY
    - helper:diff-snapshot=2 files changed, 77 insertions(+), 26 deletions(-)
    - pointer: inline — script verifies the dataclass API; note `python3 -m pytest _tests/test_provider_commands.py` currently reports 2 expected failures until tests are updated in Loop 031.
  - removal | 2026-01-02T00:39:25Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:lib/providerCommands.py'], text=True)
      if 'BarCliPayload' in content:
          raise SystemExit('BarCliPayload already present in baseline')
      raise SystemExit('baseline missing BarCliPayload dataclass (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting to baseline removes the dataclass and reproduces the red failure.
- rollback_plan: git checkout HEAD -- lib/providerCommands.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md
- delta_summary: helper:diff-snapshot=2 files changed, 77 insertions(+), 26 deletions(-); `_parse_bar_cli_payload` now returns a `BarCliPayload` dataclass consumed by `_delegate_to_bar_cli`.
- loops_remaining_forecast: 3 loops remaining (dataclass-aware tests, documentation updates, alert handling); confidence medium.
- residual_risks:
  - `_tests/test_provider_commands.py` still unpacks tuples and currently fails; Loop 031 will align tests with the dataclass return type.
- next_work:
  - Behaviour: update guardrail tests for the dataclass payload — python3 -m pytest _tests/test_provider_commands.py — future-shaping: ensure test suite validates the new helper interface.
