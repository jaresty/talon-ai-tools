- helper_version: helper:v20251223.1
- focus: ADR-0063 §Implementation Plan & Operational Mitigations (document BarCliPayload usage)
- riskiest_assumption: Without doc updates, contributors may bypass the shared dataclass and reintroduce drift (probability medium, impact medium on maintainability).
- validation_targets:
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text().lower()
      if 'barclipayload' not in text:
          raise SystemExit('README still missing payload dataclass note')
      print('README references payload dataclass')
    PY
  - python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'BarCliPayload' not in text:
          raise SystemExit('ADR still missing BarCliPayload reference')
      print('ADR documents BarCliPayload helper')
    PY
- evidence:
  - red | 2026-01-02T00:46:05Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text().lower()
      if 'barclipayload' in text:
          raise SystemExit('README already references payload dataclass')
      raise SystemExit('README missing payload dataclass note')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — README lacked dataclass guidance prior to the change.
  - red | 2026-01-02T00:46:07Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'BarCliPayload' in text:
          raise SystemExit('ADR already referenced BarCliPayload')
      raise SystemExit('ADR missing BarCliPayload reference')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — ADR lacked explicit dataclass mention before edits.
  - green | 2026-01-02T00:47:35Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('cli/README.md').read_text().lower()
      if 'barclipayload' not in text:
          raise SystemExit('README still missing payload dataclass note')
      print('README references payload dataclass')
    PY
    - helper:diff-snapshot=2 files changed, 3 insertions(+), 3 deletions(-)
    - pointer: inline — README now highlights the dataclass helper.
  - green | 2026-01-02T00:47:37Z | exit 0 | python3 - <<'PY'
      from pathlib import Path
      text = Path('docs/adr/0063-go-cli-single-source-of-truth.md').read_text()
      if 'BarCliPayload' not in text:
          raise SystemExit('ADR still missing BarCliPayload reference')
      print('ADR documents BarCliPayload helper')
    PY
    - helper:diff-snapshot=2 files changed, 3 insertions(+), 3 deletions(-)
    - pointer: inline — ADR references the dataclass and ties it to testing requirements.
  - removal | 2026-01-02T00:47:45Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:cli/README.md'], text=True)
      if 'BarCliPayload' in content:
          raise SystemExit('README baseline already referenced BarCliPayload')
      raise SystemExit('README baseline lacks BarCliPayload reference (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting README removes the dataclass note and recreates the red failure.
  - removal | 2026-01-02T00:47:48Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:docs/adr/0063-go-cli-single-source-of-truth.md'], text=True)
      if 'BarCliPayload' in content:
          raise SystemExit('ADR baseline already referenced BarCliPayload')
      raise SystemExit('ADR baseline lacks BarCliPayload reference (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting ADR removes the dataclass note and recreates the red failure.
- rollback_plan: git checkout HEAD -- cli/README.md docs/adr/0063-go-cli-single-source-of-truth.md docs/adr/0063-go-cli-single-source-of-truth.work-log.md
- delta_summary: helper:diff-snapshot=2 files changed, 3 insertions(+), 3 deletions(-); README/ADR now reference the `BarCliPayload` dataclass and `has_payload` helper.
- loops_remaining_forecast: 1 loop remaining (alert handling); confidence high.
- residual_risks:
  - Alert field behaviour still pending implementation; will be addressed next.
- next_work:
  - Behaviour: add handling for alert field in payload — python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/providerCommands.py').read_text()
      if 'payload_info.alert' not in text:
          raise SystemExit('alert field handling missing')
      print('alert field handling present')
    PY — future-shaping: ensure CLI surface can raise alert notifications.
