- helper_version: helper:v20251223.1
- focus: ADR-0063 §Talon Adapter Layer (log JSON decode failures once)
- riskiest_assumption: Without explicit logging, malformed payloads could hide CLI issues (probability medium, impact medium on observability).
- validation_targets:
  - python3 - <<'PY'
      from types import SimpleNamespace
      from pathlib import Path
      import sys
      sys.path.insert(0, str(Path('.').resolve()))
      from bootstrap import bootstrap
      bootstrap()
      from talon_user.lib import providerCommands
      payload = providerCommands._parse_bar_cli_payload(SimpleNamespace(stdout='not json'))
      if not payload.decode_failed:
          raise SystemExit('decode_failed flag not set')
      print('decode failure flagged')
    PY
- evidence:
  - red | 2026-01-02T00:52:15Z | exit 1 | python3 - <<'PY'
      from pathlib import Path
      text = Path('lib/providerCommands.py').read_text()
      if 'decode_failed' in text:
          raise SystemExit('decode failure logging already present')
      raise SystemExit('parse failure logging missing')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — confirmed decode logging absent before change.
  - green | 2026-01-02T00:53:42Z | exit 0 | python3 - <<'PY'
      from types import SimpleNamespace
      from pathlib import Path
      import sys
      sys.path.insert(0, str(Path('.').resolve()))
      from bootstrap import bootstrap
      bootstrap()
      from talon_user.lib import providerCommands
      payload = providerCommands._parse_bar_cli_payload(SimpleNamespace(stdout='not json'))
      if not payload.decode_failed:
          raise SystemExit('decode_failed flag not set')
      print('decode failure flagged')
    PY
    - helper:diff-snapshot=1 file changed, 24 insertions(+), 6 deletions(-)
    - pointer: inline — helper marks decode failure and `_delegate_to_bar_cli` logs once.
  - removal | 2026-01-02T00:53:50Z | exit 1 | python3 - <<'PY'
      import subprocess
      content = subprocess.check_output(['git', 'show', 'HEAD:lib/providerCommands.py'], text=True)
      if 'payload decode failed' in content:
          raise SystemExit('decode logging already present in baseline')
      raise SystemExit('baseline missing decode failure logging (expected removal)')
    PY
    - helper:diff-snapshot=0 files changed
    - pointer: inline — reverting baseline removes the decode failure log.
- rollback_plan: git checkout HEAD -- lib/providerCommands.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md
- delta_summary: helper:diff-snapshot=1 file changed, 24 insertions(+), 6 deletions(-); BarCliPayload tracks `decode_failed`, and `_delegate_to_bar_cli` logs malformed stdout once.
- loops_remaining_forecast: 1 loop remaining (decode logging tests); confidence high.
- residual_risks:
  - Need guardrail tests confirming decode logging (Loop 036).
- next_work:
  - Behaviour: ensure helper logs decode failures via tests — python3 -m pytest _tests/test_provider_commands.py — future-shaping: cover the new logging path.
