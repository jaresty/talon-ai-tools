## metadata
- helper_version: helper:v20251223.1
- focus: Implementation Guardrails → Delivery posture — publish signature metadata artefact paths for release guardrails (`python3 scripts/tools/check_cli_assets.py`).
- riskiest_assumption: If release guardrails do not expose the signatures metadata path, CI cannot upload `signatures.json`, leaving Talon bootstrap without auditable provenance even though local checks pass.
- validation_targets:
  - `python3 scripts/tools/check_cli_assets.py`
- rollback_plan: `git restore --source=HEAD scripts/tools/package_bar_cli.py scripts/tools/check_cli_assets.py _tests/test_check_cli_assets.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md && rm docs/adr/evidence/0063/loop-0039.md`
- delta_summary: helper:diff-snapshot=4 files changed, 18 insertions(+), 7 deletions(-) — add metadata path printing to package/guard scripts, teach guard tests to expect artefact listings, and log the loop in the work-log.
- loops_remaining_forecast:
  - 1 loop — Emit signing key drift telemetry and guard on stale metadata (`python3 scripts/tools/check_cli_assets.py`), confidence medium.
- residual_risks:
  - Signing key drift telemetry is still absent (severity medium). Mitigation: next slice records key IDs in telemetry and alerts when guard output differs from runtime expectations; monitor guard output for unexpected key rotations during CI runs.
- next_work:
  - Behaviour: Replace stubbed CLI delegation with real binary — surface signing metadata drift through telemetry fallback (`python3 -m pytest _tests/test_cli_talon_parity.py`).
  - Behaviour: Harden release pipeline with checksum manifest — enforce telemetry on signing key freshness (`python3 scripts/tools/check_cli_assets.py`).

## loop-0039 red | helper:rerun python3 scripts/tools/check_cli_assets.py
- 2026-01-03T16:03:05Z | exit 1
- helper:diff-snapshot=0 files changed
- failure: release guard fails when `signatures.json` is absent from artefacts | inline

```
missing signature metadata: artifacts/cli/signatures.json
```

## loop-0039 green | helper:rerun python3 scripts/tools/check_cli_assets.py
- 2026-01-03T16:14:57Z | exit 0
- helper:diff-snapshot=4 files changed, 18 insertions(+), 7 deletions(-)
- success: guard reports all artefacts plus metadata path for CI pickup | inline

```
all CLI assets present
cli_tarball=artifacts/cli/bar-darwin-arm64.tar.gz
cli_manifest=artifacts/cli/bar-darwin-arm64.tar.gz.sha256
cli_signatures=artifacts/cli/signatures.json
```

## loop-0039 removal | helper:rerun git restore --source=HEAD -- scripts/tools/package_bar_cli.py scripts/tools/check_cli_assets.py _tests/test_check_cli_assets.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md && mv artifacts/cli/signatures.json artifacts/cli/signatures.json.loop0039 && python3 scripts/tools/check_cli_assets.py
- 2026-01-03T16:18:20Z | exit 1
- helper:diff-snapshot=0 files changed
- failure returns when reverting loop changes and removing metadata | inline

```
missing signature metadata: artifacts/cli/signatures.json
```
