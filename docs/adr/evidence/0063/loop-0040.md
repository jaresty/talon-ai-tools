## metadata
- helper_version: helper:v20251223.1
- focus: Implementation Guardrails → Delivery posture — emit signing key telemetry and guard stale metadata (`python3 scripts/tools/check_cli_assets.py`).
- riskiest_assumption: Without telemetry enforcement, stale signing metadata could slip through release guardrails, leaving Talon bootstrap and CI unable to detect key rotations before delegating to untrusted binaries.
- validation_targets:
  - `python3 scripts/tools/check_cli_assets.py`
- rollback_plan: `git restore --source=HEAD scripts/tools/check_cli_assets.py _tests/test_check_cli_assets.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md docs/adr/evidence/0063/loop-0040.md`
- delta_summary: helper:diff-snapshot=4 files changed, 457 insertions(+), 146 deletions(-) — add telemetry drift guard, extend CLI asset tests, update work-log, and record the loop evidence.
- loops_remaining_forecast:
  - 1 loop — Surface signing telemetry through parity fallback and CLI adapters (`python3 -m pytest _tests/test_cli_talon_parity.py`), confidence medium.
- residual_risks:
  - Parity harness still ignores signing telemetry when deciding delegation fallback (severity medium). Mitigation: next slice wires telemetry readings into parity checks and watches guard output for unexpected key rotations during CI runs.
- next_work:
  - Behaviour: Replace stubbed CLI delegation with real binary — integrate signing telemetry fallback into parity harness (`python3 -m pytest _tests/test_cli_talon_parity.py`).
  - Behaviour: Release checksum manifest hardening — publish telemetry snapshots for CI artefact uploads and alerts (`python3 scripts/tools/check_cli_assets.py`).

## loop-0040 red | helper:rerun python3 scripts/tools/check_cli_assets.py
- 2026-01-03T16:56:03.642163+00:00 | exit 1
- helper:diff-snapshot=0 files changed
- failure: signature telemetry signing_key_id mismatch | inline

```
signature telemetry signing_key_id mismatch
```

## loop-0040 green | helper:rerun python3 scripts/tools/check_cli_assets.py
- 2026-01-03T16:56:15.212125+00:00 | exit 0
- helper:diff-snapshot=4 files changed, 457 insertions(+), 146 deletions(-)
- success: guard reports CLI artefacts and telemetry status | inline

```
all CLI assets present
cli_tarball=artifacts/cli/bar-darwin-arm64.tar.gz
cli_manifest=artifacts/cli/bar-darwin-arm64.tar.gz.sha256
cli_signatures=artifacts/cli/signatures.json
```

## loop-0040 removal | helper:rerun python3 scripts/tools/check_cli_assets.py
- 2026-01-03T16:56:28.656987+00:00 | exit 1
- helper:diff-snapshot=0 files changed
- failure returns after reintroducing stale telemetry | inline

```
signature telemetry signing_key_id mismatch
```
