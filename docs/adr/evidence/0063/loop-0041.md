## metadata
- helper_version: helper:v20251223.1
- focus: Implementation Guardrails → Delivery posture — enforce signature telemetry fallback in bootstrap parity harness (`python3 -m pytest _tests/test_cli_talon_parity.py`).
- riskiest_assumption: Without verifying release signing telemetry before delegation, Talon can trust CLI binaries signed with stale keys, leaving adapters and parity checks blind to tampering.
- validation_targets:
  - `python3 -m pytest _tests/test_cli_talon_parity.py`
- rollback_plan: `git restore --source=HEAD _tests/test_cli_talon_parity.py bootstrap.py lib/bootstrapTelemetry.py scripts/tools/install_bar_cli.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md docs/adr/evidence/0063/loop-0041.md`
- delta_summary: helper:diff-snapshot=6 files changed, 358 insertions(+), 2 deletions(-) — add signing telemetry verification to bootstrap/install, extend parity harness coverage, and log the loop.
- loops_remaining_forecast:
  - 1 loop — Route signing telemetry status into adapter UX and parity fallback alerts (`python3 -m pytest _tests/test_cli_talon_parity.py`), confidence medium.
- residual_risks:
  - Adapter notifications still present signing telemetry mismatches as generic bootstrap warnings (severity medium). Mitigation: wire telemetry status into adapter drop reasons and provider canvases; monitor parity harness output for repeated mismatches.
- next_work:
  - Behaviour: Replace stubbed CLI delegation with real binary — propagate telemetry mismatch state through adapters and provider canvases (`python3 -m pytest _tests/test_cli_talon_parity.py`).
  - Behaviour: Release checksum manifest hardening — surface telemetry snapshots in CI drift reports (`python3 scripts/tools/check_cli_assets.py`).

## loop-0041 red | helper:rerun python3 -m pytest _tests/test_cli_talon_parity.py
- 2026-01-03T17:19:45Z | exit 1
- helper:diff-snapshot=0 files changed
- failure: parity harness allowed delegation despite stale signature telemetry when the bootstrap guard was temporarily disabled for reproduction | inline

```
E           AssertionError: True is not false : Bootstrap should disable delegation when signing telemetry mismatches
=========================== short test summary info ============================
FAILED _tests/test_cli_talon_parity.py::CLITalonParityTests::test_bootstrap_disables_delegation_on_signature_telemetry_mismatch
```

## loop-0041 green | helper:rerun python3 -m pytest _tests/test_cli_talon_parity.py
- 2026-01-03T17:21:51.747132+00:00 | exit 0
- helper:diff-snapshot=6 files changed, 358 insertions(+), 2 deletions(-)
- success: parity harness now disables delegation on telemetry drift and all tests pass | inline

```
_tests/test_cli_talon_parity.py ..........                               [100%]
```

## loop-0041 removal | helper:rerun python3 -m pytest _tests/test_cli_talon_parity.py
- 2026-01-03T17:21:15Z | exit 1
- helper:diff-snapshot=0 files changed
- failure returns after temporarily removing telemetry verification to restore pre-fix behaviour | inline

```
E           AssertionError: True is not false : Bootstrap should disable delegation when signing telemetry mismatches
=========================== short test summary info ============================
FAILED _tests/test_cli_talon_parity.py::CLITalonParityTests::test_bootstrap_disables_delegation_on_signature_telemetry_mismatch
```
