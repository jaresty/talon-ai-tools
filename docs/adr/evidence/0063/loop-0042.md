## metadata
- helper_version: helper:v20251223.1
- focus: Implementation Guardrails → Delivery posture — surface signing telemetry mismatch through adapter UX (`python3 -m pytest _tests/test_cli_talon_parity.py`).
- riskiest_assumption: If CLI delegation continues to report generic health probe failures during signing drift, operators will miss telemetry mismatch recovery steps and keep running unsigned binaries.
- validation_targets:
  - `python3 -m pytest _tests/test_cli_talon_parity.py`
- rollback_plan: `git restore --source=HEAD _tests/test_cli_talon_parity.py lib/providerRegistry.py lib/requestGating.py lib/requestLog.py lib/requestState.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md docs/adr/evidence/0063/loop-0042.md`
- delta_summary: helper:diff-snapshot=5 files changed, 46 insertions(+), 5 deletions(-) — add signature telemetry drop reason, propagate messages to provider status, update parity harness, and log the loop.
- loops_remaining_forecast:
  - 1 loop — Connect delegation telemetry recovery prompts into provider canvases and history surfaces (`python3 -m pytest _tests/test_cli_talon_parity.py`), confidence medium.
- residual_risks:
  - Provider canvases still show generic rebuild text when telemetry recovers (severity medium). Mitigation: next slice synchronises telemetry status with provider UI refresh and tracks recovery events in parity harness.
- next_work:
  - Behaviour: Replace stubbed CLI delegation with real binary — feed telemetry recovery prompts into provider canvases and adapter notifications (`python3 -m pytest _tests/test_cli_talon_parity.py`).
  - Behaviour: Harden release pipeline with checksum manifest — export telemetry recovery summaries for CI artefacts (`python3 scripts/tools/check_cli_assets.py`).

## loop-0042 red | helper:rerun PYTHONPATH=var/tmp python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_bootstrap_disables_delegation_on_signature_telemetry_mismatch
- 2026-01-03T19:33:20.555931+00:00 | exit 1
- helper:diff-snapshot=0 files changed
- failure: gating still returns `cli_unhealthy`, leaving signature telemetry mismatch undetected | inline

```
E           AssertionError: 'cli_unhealthy' != 'cli_signature_mismatch'
E           - cli_unhealthy
E           + cli_signature_mismatch
```

## loop-0042 green | helper:rerun python3 -m pytest _tests/test_cli_talon_parity.py
- 2026-01-03T19:35:26.696557+00:00 | exit 0
- helper:diff-snapshot=5 files changed, 46 insertions(+), 5 deletions(-)
- success: parity harness reflects signature telemetry mismatch in gating and provider status | inline

```
_tests/test_cli_talon_parity.py ..........                               [100%]
```

## loop-0042 removal | helper:rerun PYTHONPATH=var/tmp python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_bootstrap_disables_delegation_on_signature_telemetry_mismatch
- 2026-01-03T19:35:52.162133+00:00 | exit 1
- helper:diff-snapshot=0 files changed
- failure returns when signature telemetry detection is bypassed, confirming guardrail coverage | inline

```
E           AssertionError: 'cli_unhealthy' != 'cli_signature_mismatch'
E           - cli_unhealthy
E           + cli_signature_mismatch
```
