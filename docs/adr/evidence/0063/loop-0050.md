## metadata
- helper_version: helper:v20251223.1
- focus: Implementation Guardrails → Delivery posture — export refreshed signature telemetry bundles for CI uploads (`python3 -m pytest _tests/test_check_cli_assets.py`)
- riskiest_assumption: Without copying the regenerated signature telemetry to a stable artefact path, CI uploads remain stale even after automatic packaging, leaving Talon operators without updated recovery prompts.
- validation_targets:
  - `python3 -m pytest _tests/test_check_cli_assets.py`
- rollback_plan: `git restore --source=HEAD scripts/tools/check_cli_assets.py _tests/test_check_cli_assets.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md` then rerun the validation command to confirm the exported telemetry path disappears.
- delta_summary: helper:diff-snapshot=3 files changed, 145 insertions(+), 5 deletions(-) — copy signature telemetry to an artefact export, emit CI upload path, and extend guard tests and work-log.
- loops_remaining_forecast:
  - 1 loop — integrate Talon parity UX remediation for recovery snapshot drift (`python3 -m pytest _tests/test_cli_talon_parity.py`), confidence medium.
  - 1 loop — document operator guidance for CI uploads consuming the exported telemetry (`python3 -m pytest _tests/test_check_cli_assets.py`), confidence medium.
- residual_risks:
  - Release automation must still upload the exported telemetry file; add CI workflow steps to publish `artifacts/cli/signature-telemetry.json` and monitor guard output for missing uploads (severity medium).
- next_work:
  - Behaviour: Harden release pipeline with checksum manifest — update CI scripts to push `artifacts/cli/signature-telemetry.json` alongside other artefacts (`python3 scripts/tools/check_cli_assets.py`).

## loop-0050 red | helper:rerun python3 -m pytest _tests/test_check_cli_assets.py
- 2026-01-03T23:55:00Z | exit 1
- helper:diff-snapshot=2 files changed, 8 insertions(+), 0 deletions(-)
- failure: guard output lacks exported telemetry path; new test expects copy | inline

```
E       AssertionError: 'cli_signature_telemetry=/var/.../signature-telemetry.json' not found in 'all CLI assets present\ncli_tarball=artifacts/cli/bar-darwin-arm64.tar.gz\ncli_manifest=artifacts/cli/bar-darwin-arm64.tar.gz.sha256\ncli_signatures=/var/.../signatures.json\n'
```

## loop-0050 green | helper:rerun python3 -m pytest _tests/test_check_cli_assets.py
- 2026-01-03T23:59:00Z | exit 0
- helper:diff-snapshot=3 files changed, 145 insertions(+), 5 deletions(-)
- success: guard copies telemetry to artefact export and tests confirm CI path | inline

```
_tests/test_check_cli_assets.py .................                        [100%]
```

## loop-0050 removal | helper:rerun git restore --source=HEAD scripts/tools/check_cli_assets.py && python3 -m pytest _tests/test_check_cli_assets.py
- 2026-01-04T00:04:00Z | exit 1
- helper:diff-snapshot=0 files changed
- failure returns after rollback: exported telemetry artefact missing | inline

```
E       AssertionError: 'cli_signature_telemetry=/var/.../signature-telemetry.json' not found in ''
```
