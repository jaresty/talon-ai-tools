- helper_version: helper:v20251223.1
- focus: Implementation Guardrails → Delivery posture — guardrail rejects digest-only tarball manifests
- riskiest_assumption: If the checksum manifest omits the packaged filename, release guardrails would read a valid digest yet Talon operators could ship the wrong artefact unnoticed.
- validation_targets:
  - `python3 -m pytest _tests/test_check_cli_assets.py`
- rollback_plan: `git restore --source=HEAD scripts/tools/check_cli_assets.py _tests/test_check_cli_assets.py docs/adr/0063-go-cli-single-source-of-truth.work-log.md docs/adr/evidence/0063/loop-0058.md`
- delta_summary: helper:diff-snapshot=3 files changed, 42 insertions(+), 3 deletions(-) — tighten manifest parser, extend guard tests, and log the loop in the work log.
- loops_remaining_forecast:
  - 1 loop — Capture CI artefact evidence once upload step is exercised, confidence medium.
- residual_risks:
  - Packaging could still emit malformed manifests (e.g., truncated files); the guard now fails loudly, but CI must treat the failure as blocking.
- next_work:
  - Behaviour: Harden release pipeline with checksum manifest — capture CI upload evidence for `artifacts/cli/signature-telemetry.json` once available (`python3 scripts/tools/check_cli_assets.py`).

## loop-0058 red | helper:rerun python3 -m pytest _tests/test_check_cli_assets.py
- 2026-01-04T03:32:00Z | exit 1
- helper:diff-snapshot=3 files changed, 40 insertions(+), 1 deletion(-)
- failure: `test_manifest_requires_filename` reports checksum mismatch because the guard accepts a digest-only manifest string | inline

## loop-0058 green | helper:rerun python3 -m pytest _tests/test_check_cli_assets.py
- 2026-01-04T03:37:00Z | exit 0
- helper:diff-snapshot=3 files changed, 42 insertions(+), 3 deletions(-)
- summary: manifest parser now trims and enforces the filename, and the guard/test suite pass with the stricter validation | inline

## loop-0058 removal | helper:rerun python3 -m pytest _tests/test_check_cli_assets.py
- 2026-01-04T03:40:00Z | exit 1
- helper:diff-snapshot=3 files changed, 40 insertions(+), 1 deletion(-)
- failure: Reintroducing the prior parser allows digest-only manifests again, and the checksum mismatch surfaces immediately | inline
