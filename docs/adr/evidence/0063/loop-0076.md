- helper_version: helper:v20251223.1
- focus: Implementation Guardrails → Delivery posture — Gate provider command execution when parity disables shell-outs
- riskiest_assumption: Without an explicit feature flag, the CLI still executes the external provider command even when parity requires fixtures-only mode (`python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_cli_delegate_skips_provider_command_when_disabled`).
- validation_targets:
  - `python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_cli_delegate_skips_provider_command_when_disabled`
  - `python3 -m pytest _tests/test_cli_talon_parity.py`
- rollback_plan: `git restore --source=HEAD _tests/test_cli_talon_parity.py cmd/bar/main.go docs/adr/0063-go-cli-single-source-of-truth.md docs/adr/0063-go-cli-single-source-of-truth.work-log.md bin/bar.bin` and rerun `python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_cli_delegate_skips_provider_command_when_disabled` to confirm the failure returns.
- delta_summary: helper:diff-snapshot=5 files changed, 108 insertions(+), 1 deletion(-) — add parity coverage for disabled provider commands, gate shell-outs via `BAR_PROVIDER_COMMAND_MODE`, update ADR guardrails/work-log, and rebuild the CLI binary.
- loops_remaining_forecast:
  - 1 loop — replace shell-out transport with native provider client and maintain dual evidence (`python3 -m pytest _tests/test_cli_talon_parity.py`); confidence medium pending client scaffolding.
- residual_risks:
  - CLI still shells out when mode is `auto` (severity medium); mitigation: land native client and keep telemetry watchdogs to disable delegation when transport regresses.
- next_work:
  - Behaviour: Introduce provider client interface with deterministic fixture recorder — `python3 -m pytest _tests/test_cli_talon_parity.py`.

## loop-0076 red | python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_cli_delegate_skips_provider_command_when_disabled | 2026-01-05T05:04:18Z | exit 1
- helper:diff-snapshot=2 files changed, 106 insertions(+)
- failure: CLI still executed the external provider command; response message stayed "provider command" instead of fallback summary when the mode was disabled | inline

## loop-0076 removal | git restore --source=HEAD _tests/test_cli_talon_parity.py cmd/bar/main.go docs/adr/0063-go-cli-single-source-of-truth.md docs/adr/0063-go-cli-single-source-of-truth.work-log.md bin/bar.bin && python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_cli_delegate_skips_provider_command_when_disabled | 2026-01-05T05:04:18Z | exit 1
- helper:diff-snapshot=2 files changed, 106 insertions(+)
- failure: Restoring the pre-loop CLI allowed the provider command to run even in disabled mode, reproducing the missing fallback expectations | inline

## loop-0076 green | python3 -m pytest _tests/test_cli_talon_parity.py | 2026-01-05T05:08:18Z | exit 0
- helper:diff-snapshot=5 files changed, 108 insertions(+), 1 deletion(-)
- summary: Parity suite passes with provider command feature flag gating in place and ADR/work-log updates recorded | inline
