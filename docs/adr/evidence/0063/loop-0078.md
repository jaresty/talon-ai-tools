- helper_version: helper:v20251223.1
- focus: Implementation Guardrails → Delivery posture — Fixtures-only mode must fail when transcripts are missing
- riskiest_assumption: `BAR_PROVIDER_COMMAND_MODE=fixtures-only` still shells out or returns synthetic output when no recorded transcript exists (`python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_cli_fixture_only_mode_errors_without_transcript`).
- validation_targets:
  - `python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_cli_fixture_only_mode_errors_without_transcript`
  - `python3 -m pytest _tests/test_cli_talon_parity.py`
- rollback_plan: `git restore --source=HEAD _tests/test_cli_talon_parity.py cmd/bar/main.go bin/bar.bin` and rerun `python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_cli_fixture_only_mode_errors_without_transcript` to confirm the fixtures-only path regresses.
- delta_summary: helper:diff-snapshot=3 files changed, 96 insertions(+), 5 deletions(-) — add parity coverage for fixtures-only missing transcripts, surface structured CLI errors with non-zero exit codes, and rebuild the binary.
- loops_remaining_forecast:
  - 1 loop — implement native provider client with deterministic recordings and auto-refresh telemetry (`python3 -m pytest _tests/test_cli_talon_parity.py`); confidence medium pending transport scaffolding.
- residual_risks:
  - Recorded transcript catalog can drift (severity medium); mitigation: add tooling to refresh fixtures and CI guard enforcing transcript freshness.
- next_work:
  - Behaviour: Introduce provider transport interface with fixture recorder — `python3 -m pytest _tests/test_cli_talon_parity.py`.

## loop-0078 red | python3 -m pytest _tests/test_cli_talon_parity.py::CLITalonParityTests::test_cli_fixture_only_mode_errors_without_transcript | 2026-01-05T05:23:45Z | exit 1
- helper:diff-snapshot=2 files changed, 52 insertions(+)
- failure: Fixtures-only mode still returned synthetic output, so the parity assertion expecting a failure saw `success=True` | inline

## loop-0078 green | python3 -m pytest _tests/test_cli_talon_parity.py | 2026-01-05T05:27:45Z | exit 0
- helper:diff-snapshot=3 files changed, 96 insertions(+), 5 deletions(-)
- summary: Fixtures-only missing transcripts now exit non-zero with structured error, full parity suite passes | inline
