# API Specification

## completeness

Every endpoint listed in this specification includes a request schema, a response schema, and at least one example. Partial endpoint entries are not permitted.

## authentication

Authentication uses bearer tokens in the `Authorization` header.

## versioning

All endpoints are versioned under `/v1/`.
