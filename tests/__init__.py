"""Compatibility package for ``python -m unittest discover -s tests``.

The real test suite lives in the top-level ``_tests`` package. See
``tests/test_redirect.py`` for the discovery hook that forwards to it.
"""

